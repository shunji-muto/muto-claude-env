## タイプ
<!-- Routine契約: 実装型(テストあり) / UI仮実装 のいずれか -->

## 完了条件
<!-- 「X の時 Y になる」がテスト可能な文で。UI 仮実装の場合は「以下の画面遷移で正常動作すること」等 -->

## 変更対象ファイル
<!-- 相対パス列挙 + [New] / [Edit] / [Delete] ラベル -->

## 参考にする既存パターン
<!-- 「packages/xxx/src/aaa.ts と同じ構造で」等の具体指示 or「なし・自明」 -->

## 触ってはいけない
<!-- 個別禁止領域があれば。共通禁止パスは Routine プロンプト側で強制するので通常「なし」 -->

## テストケース(Routine契約 - 改変禁止)
<!-- テストありケースのみ。「should ... when ...」形式で列挙。
     この Issue が Routine で実装される際、実装後のテストファイルにこの名前が全部含まれることを
     Routine 側プロンプトが機械照合する(後続タスクの pick-and-implement Routine プロンプトで実装予定)。
     Skill 単体では照合器を持たないので、Routine プロンプト側の実装がリリースされて初めて契約が発効する。 -->

- `should ...`
- `should ...`

## 手動確認手順(UI 仮実装のみ)
<!-- UI 仮実装ケースのみ。実行手順を番号付きで -->
